const express = require("express");
const router = express.Router();
const { User, Board, Calender, Notes } = require("../../models");

// router.get("/", (req,res) => {
//     Notes.findAll({

//     }).then(notes => {
//         console.log(notes)
//         const
//     })
// })
